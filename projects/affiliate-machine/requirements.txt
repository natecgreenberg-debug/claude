# Affiliate Machine - Python Dependencies
# Install when pip is available: pip install -r requirements.txt

httpx==0.27.0          # HTTP client for API calls
python-dotenv==1.0.1   # Load .env secrets
